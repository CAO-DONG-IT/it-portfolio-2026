# Console Todo（概要）

- v0.1：標準入力 → 空判定 → 出力（起動確認）
- v0.2：メニュー＋追加／一覧を実装（README＋スクリーンショットで証跡化）
- v0.3：入力校验（全角/英字/範囲外を検知）＋番号指定の削除を追加し、削除後に一覧を再表示して操作性を改善（操作ログ＋スクリーンショットで証跡化）
- v0.4（予定）：ファイル保存／読み込み
- v0.5（予定）：リファクタリング／構造化（責務分離・メソッド整理）

