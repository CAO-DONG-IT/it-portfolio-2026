# Java 構成（現状：一時停止）

## 位置づけ
現在は Network（CCNA）と Linux を優先しているため、Java の新規学習・改修は一時停止している。  
ただし、基礎的な実装学習とドキュメント化の証跡として、既存成果物（ConsoleTodo）は保持する。

## ディレクトリ
- `ConsoleTodo/`
  - `README.md`（入口：実行手順・バージョン導線）
  - `v0.1/`（起動確認）
  - `v0.2/`（メニュー／追加・一覧）
  - `v0.3/`（機能追加版）
- `notes/`
  - `java-basics.md`（基礎メモ）
- `screenshots/`
  - `README.md`（証跡ルール）
  - スクリーンショット一式

## 運用ルール（最小）
- 既存成果物は削除せず、参照できる状態で保持する
- 各バージョンの README に実行手順と操作例を残す
- 証跡が必要な場合は `screenshots/` に保存し、README から参照する
- 学習を再開した場合は `weekly-log` に記録し、成果物パスを残す

## 入口
- Java：`java/README.md`
- ConsoleTodo：`java/ConsoleTodo/README.md`

## 今後の再開時メモ
- ConsoleTodo を継続する場合は、入力チェックや例外処理などを段階的に追加する
- 変更点は README に追記し、必要に応じて証跡も更新する
